---
name: cnkidata
description: 从中国知网（CNKI）批量采集并导出结构化文献题录。适用于按期刊、主题、作者、单位或基金检索指定年份范围，逐页选择结果，导出18列数据，并完成来源过滤、去重与完整性核验；不用于下载论文全文或处理单篇引用。
---

# CNKI 批量题录采集

把 CNKI 检索结果导出为结构化表格，默认保留 18 个字段：来源库、题名、作者、单位、文献来源、关键词、摘要、发表时间、第一责任人、基金、年、卷、期、页码、中图分类号、ISSN、URL、DOI。

## 开始前

从用户请求中识别检索字段、检索词、年份范围和排序方式。已经明确的内容不要重复询问；排序未指定时用发表时间降序。期刊名可能存在标点差异或更名史，必要时先用 CNKI 期刊检索核准。

执行前读取：

- [references/iron-rules.md](references/iron-rules.md)：所有任务都要遵守的数据质量规则。
- [references/workflow-code.md](references/workflow-code.md)：需要实际操作 CNKI 页面、导出或合并文件时读取。

## 执行流程

1. 按用户指定字段检索，并通过自动补全中的精确选项锁定目标来源。
2. 限定年份范围，设置每页 50 条及发表时间降序。
3. 先导出一页，确认 `Source-文献来源` 只有目标值。
4. 每批最多选择 500 条；逐页选择时依据整页所有日期判断是否越过目标年份。
5. 在主页面和导出页面都精确选择“自定义”，全选字段后下载 xls。
6. 遇到登录或验证码时暂停，明确告诉用户需要在浏览器中完成什么；用户完成后继续，不绕过验证。
7. 合并批次，严格按来源精确过滤，按题名去重，限定目标年份并核验期号和篇数。
8. 保存最终 xlsx，并报告总篇数、各期篇数、字段数、来源唯一值和重复记录数。

## 工具选择

优先使用当前环境已有的浏览器自动化工具。Codex 桌面环境没有浏览器 MCP 时，使用 `agent-browser` CLI；先运行 `agent-browser skills get core`，再遵循快照、交互、重新快照的循环。复用用户已登录的浏览器会话；不要读取、回显或保存用户凭据。

浏览器工具不可用时，先执行只读诊断并说明具体阻塞点。不要改用来源不明的第三方数据冒充 CNKI 结果。

## 完成标准

- 最终表为 18 列，列名与参考文件一致。
- `Source-文献来源` 唯一且精确等于目标刊名。
- 年份全部落在用户指定范围内。
- 题名无重复。
- 期号覆盖与期刊出版周期相符；异常低发文量必须复查。
