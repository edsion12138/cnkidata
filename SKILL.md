---
name: cnkidata
description: 从知网(CNKI)批量导出文献数据为18列xlsx表格。核心场景是"按期刊/文献来源批量导出某时间段全部发文"，也支持任意定制检索（主题、作者、关键词、单位、基金等字段组合 + 年份范围 + 排序方式）后的逐页全选与导出。当用户提到爬知网、批量导出期刊发文、导出某期刊某年份所有论文、知网文献数据采集、CNKI 数据导出、逐页勾选导出等，即使没明确说"用skill"，只要是要从知网批量拿结构化数据而非下载PDF或单篇引用，就应该用本skill。内置实战总结的检索设计、同名期刊精准锁定、跨页累积全选、自定义18列导出、合并去重核验全流程铁律。
---

# CNKI 批量数据导出（cnkidata）

从知网批量导出结构化文献数据为 18 列 xlsx。检索设计可定制，导出后的合并、去重、核验是本 skill 的独有增值。

## 什么时候用

- 按**期刊/文献来源**导出某时间段全部发文（如"某CSSCI期刊2020-2026年所有论文"）
- 按任意检索式（主题、篇名、作者、单位、基金等字段组合 + 年份范围）批量导出
- 需要的是**18列结构化数据**（题名、作者、单位、来源、关键词、摘要、发表时间、基金、卷期页码、中图分类号、ISSN、DOI等），而不是 PDF 全文或单篇引用（那用 cnki-download / cnki-export）

## 核心流程（五步）

```
1. 检索设计 → 2. 执行检索+精准锁定 → 3. 核验来源纯净度 → 4. 逐页全选+批量导出 → 5. 合并去重核验
```

### 第1步：检索设计（定制化核心）

先和用户确认四个要素，缺一不可：

| 要素 | 说明 | 示例 |
|------|------|------|
| 检索字段 | 文献来源 / 主题 / 篇名 / 作者 / 单位 / 基金 | 文献来源（期刊） |
| 检索词 | 目标期刊名、关键词、作者名等 | "高等教育研究" |
| 年份范围 | 起止年 | 2020-2026 |
| 排序方式 | 发表时间降序（默认） | 发表时间 |

**检索词准确性是最大坑**：CNKI 刊名可能用句点而非间隔号（"课程.教材.教法"不是"课程·教材·教法"），期刊可能更名（"国际中文教育(中英文)"2020年旧名"国际汉语教育(中英文)"）。检索前务必用 `cnki-journal-search` 或让用户确认准确刊名。

### 第2步：执行检索 + 精准锁定

1. 点检索框字段下拉 `.sort-default` → 选目标字段（如 `li[data-val="LY"]` 文献来源）→ **再点一次 `.sort-default` 折叠下拉**（否则遮挡全选框）
2. 输入检索词，等自动补全
3. **同名混淆刊必须勾选精准复选框**：当刊名是其他刊名子串（如"教育学报"会混入"比较教育学报""教师教育学报"）时，勾选自动补全里精确刊名左侧的复选框锁定
4. 折叠下拉 → 点搜索

### 第3步：核验来源纯净度

**不要直接批量导出**。先只勾选第1页，导出，检查 `Source-文献来源` 列是否唯一且等于目标。混刊立即回第2步加复选框。

### 第4步：逐页全选 + 批量导出

1. 设置 50 条/页（点 `#perPageDiv a` 选"50"）
2. 排序按发表时间降序（点"发表时间"两次）
3. **分批**：每批约 10 页（450-500条，CNKI 上限500），逐页勾选 `#selectCheckAll1`（跨页累积，不丢失）
4. **停止判断**：看页面**所有年份**，只要还有 ≥2020（或目标起始年）的论文就继续选，只有整页都 < 目标起始年才停止。不能只看首篇年份
5. 每批导出：导出与分析 → 导出文献 → **精确文本匹配"自定义"**（不是"查新（自定义引文格式）"）→ 导出页再选"自定义"格式 → 全选字段 → xls
6. **点 xls 后等 15 秒**（验证码在此刻弹出，需用户手动点击，停下等用户）
7. 批次间导航：清除后用"首页 + 精准翻页"定位到下一批起始页，**不要依赖连续点"下一页"**（会跳页）

### 第5步：合并去重核验

所有批次导出后：

1. **严格按来源名精确过滤**：只保留 `Source-文献来源 == 目标刊名` 的行（模糊匹配会混入他刊）
2. 合并所有批次 → 按"Title-题名"去重
3. **逐本核验三要素**：字段18列、来源单一、年份覆盖完整（缺年份或某年篇数异常偏低都要回去补）
4. 最终保存为 `{目标名}.xlsx`

## 铁律速查

- 检索词要准确（标点、更名史）
- 同名期刊用精准复选框
- 选字段后折叠下拉
- 先验纯再批量
- 停止看整页年份不是首篇
- 导出格式精确"自定义"
- xls后等15秒（验证码）
- 批次导航用首页+精准翻页
- 合并严格按来源名过滤
- 最终按标题去重 + 逐本核验

## 详细操作与代码

具体每一步的 `page.evaluate` 代码片段、完整铁律、边角情况处理，见：

- `references/iron-rules.md` — 16条实战铁律全文
- `references/workflow-code.md` — 检索/全选/导出的 evaluate 代码模板
